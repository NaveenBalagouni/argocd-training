apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: git-appset-app
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          # 1️ Git directory generator (points to app dirs like frontend/backend)
          - git:
              repoURL: https://github.com/NaveenBalagouni/argocd-training.git   #  Update this
              revision: main
              directories:
                - path: applications/resources/git-appset-src/*     #  Matches all subdirectories

          # 2️ List generator for environments
          - list:
              elements:
                - env: dev
                  namespace: dev
                - env: prod
                  namespace: prod

  template:
    metadata:
      name: '{{path.basename}}-{{env}}'    #  Valid RFC1123 name, e.g. frontend-dev
    spec:
      project: default
      source:
        repoURL: https://github.com/NaveenBalagouni/argocd-training.git         #  Update this
        targetRevision: main
        path: '{{path}}'
        helm:
          releaseName: '{{path.basename}}-{{env}}'
          valueFiles:
            - values-{{env}}.yaml         #  Must exist in each app folder
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
        createNamespace: true
      syncPolicy:
        automated:
          prune: true
          selfHeal: true


# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: git-directory-appset
#   namespace: argocd
# spec:
#   # This generator discovers subdirectories in a Git repository
#   generators:
#   - git:
#       repoURL: https://github.com/NaveenBalagouni/argocd-training.git  # 🔁 Update with your Git repository URL
#       revision: HEAD
#       directories:
#       - path: applications/resources/git-appset-src/* # 🔁 Update with the path to your application directories

#   # This template defines how to create an Argo CD Application for each discovered directory
#   template:
#     metadata:
#       # This name uses a variable from the generator to create a unique name for each app
#       name: '{{path.basename}}'
#     spec:
#       project: default
#       source:
#         repoURL: https://github.com/NaveenBalagouni/argocd-training.git #  Same as above
#         targetRevision: HEAD
#         path: '{{path}}' # The path to the subdirectory, e.g., 'apps/my-app'
#       destination:
#         server: https://kubernetes.default.svc
#         namespace: '{{path.basename}}' # Each application will be deployed into its own namespace
#       syncPolicy:
#         automated:
#           prune: true
#           selfHeal: true
#         # This will create the namespace if it does not exist
#         syncOptions:
#         - CreateNamespace=true